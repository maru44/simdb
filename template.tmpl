// Code generated by "github.com/maru44/simdb/gen"; DO NOT EDIT.

package {{ $.PackageName }}

import (
    "pkg/errors"
    "sync"
)

type (
    {{ $.Name }} struct {
        {{ range $c := $.Columns -}}
            {{ if not $c.IsPrimaryKey }} {{ $c.Name }} {{ $c.Type }} {{ end }}
        {{ end -}}
    }

    {{ $.Name }}s struct {
        Data map[ {{ $.PrimaryKeyType }} ]{{ $.Name }}
        mux sync.Mutex
    }
)

func New{{ $.Name }}() {{ $.Name }}s {
    return {{ $.Name }}s{
        Data: map[ {{ $.PrimaryKeyType }} ]{{ $.Name }}{},
    }
}

func (t {{ $.Name }}s) Get(id {{ $.PrimaryKeyType }} ) ({{ $.Name }}, error) {
    v, ok := t.Data[id]
    if !ok {
        return {{ $.Name }}{}, errors.New("Not Exists")
    }
    return v, nil
}

func (t {{ $.Name }}s) Insert(id {{ $.PrimaryKeyType }}, value {{ $.Name }} ) error {
    if _, ok := t.Data[id]; ok {
        return errors.New("Already Exists")
    }
    t.mux.Lock()
    defer t.mux.Unlock()
    t.Data[id] = value
    return nil
}

func (t {{ $.Name }}s) BulkInsert(values {{ $.Name }}s) error {
    t.mux.Lock()
    defer t.mux.Unlock()
    for id, value := range values {
        if _, ok := t.Data[id]; ok {
            return errors.New("Already Exists")
        }
        t.Data[id] = value
    }
    return nil
}

func (t {{ $.Name }}s) Update(id {{ $.PrimaryKeyType }}, value {{ $.Name }} ) error {
   if _, ok := t.Data[id]; !ok {
        return errors.New("Not Exists")
    }
    t.mux.Lock()
    defer t.mux.Unlock()
    t.Data[id] = value
    return nil
}

func (t {{ $.Name }}s) Upsert(id {{ $.PrimaryKeyType }}, value {{ $.Name }} ) {
    t.mux.Lock()
    defer t.mux.Unlock()
    t.Data[id] = value
}

func (t {{ $.Name }}s) BulkUpsert(values {{ $.Name }}s) {
    t.mux.Lock()
    defer t.mux.Unlock()
    for id, value := range values {
        t.Data[id] = value
    }
}

func (t {{ $.Name }}s) Delete(id {{ $.PrimaryKeyType }}) {
    t.mux.Lock()
    defer t.mux.Unlock()
    delete(t, id)
}

func (t {{ $.Name }}s) Truncate() {
    t.mux.Lock()
    defer t.mux.Unlock()
    t.Data = map[ {{ $.PrimaryKeyType }} ]{{ $.Name }}{}
}
